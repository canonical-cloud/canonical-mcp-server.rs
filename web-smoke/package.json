{
  "name": "canonical-web-smoke",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright browser-smoke for the canonical.cloud live web surface. Run in CI via .github/workflows/web-smoke.yml; never a PR gate.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
