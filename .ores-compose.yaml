schema_version: ores.compose.v1
project: canonical-mcp-server
allow_lazy_start: false

services:
  canonical-mcp-server:
    name: canonical-mcp-server
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/canonical-mcp-server.rs"]
